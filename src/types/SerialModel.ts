export type SerialModel = {
  seriesNumber: string;
  cost: number;
  endDate: string | Record<string, unknown>;
  fullSeriesNumber: string;
  qty: number;
  something: string | Record<string, unknown>;
};
